{
  "scripts": {
    "serve": "live-server . --host 0.0.0.0",
    "build": "static-i18n . -l de -i en -i de -o . --outputDefault index.html --outputOther __lng__/index.html --files index.source.html"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "static-i18n": "^0.2.6"
  }
}